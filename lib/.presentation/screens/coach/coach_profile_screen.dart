import 'package:flutter/material.dart';

class CoachProfileScreen extends StatelessWidget {
  const CoachProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Coach Profile Screen 2"),
      ),
    );
  }
}
